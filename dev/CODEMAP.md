# CODEMAP · 项目代码结构（不含 dev/ 开发台）

> **干嘛**：项目真实代码在哪、各文件夹装什么,给 agent 导航。**`dev/` 是开发台(过程/计划),不在此图。**
> **怎么更新**：代码结构变了(加/删/重命名模块)就同步;**只记结构与职责、不记实现细节**(实现看代码 + `tasks/{developer_id}/done/`)。

## 仓库根
| 路径 | 装什么 |
|---|---|
| `<src>/` | <代码主体> |
| `dev/` | 开发 OS（**本图不画**） |

## <模块分组>
| 模块 | 职责 |
|---|---|
| `<module>` | <一句话职责> |
