# RULES.project · 本项目铁律【项目级别】· <项目>

> OS 通用铁律见 `RULES.md`(勿动)。这里只写**本项目**特有的红线。CLAUDE.md 会把新 agent 指到这份。

<!-- 格式·防跑偏 | 结构型【项目级别】填：固定三块——项目红线 / 致命错误即停工 / 性能·数据标准。
照本项目实际填;OS 通用铁律不写这里(在 RULES.md)。
怎么更新：① 开发者明确给出/指明本项目红线时加;② agent 遇模糊处 → 先问开发者 → 确认是项目规则后才加(agent 不自己定项目红线)。 -->

## 项目红线
- <本项目不可碰的：某文件冻结 / 某操作禁止 / 安全不变量（运行时强制）/ 单一源锚点>

## 致命错误即停工（对应 RULES.md §5）
- <出现即停工的清单：不可逆操作 / 动钱 / 安全不变量被削弱 / 数据泄露 …>

## 性能 / 数据标准
- <性能基线 / 可复现口径(同码+版本+seed→±?) / 数据质量门 / 模式安全>
